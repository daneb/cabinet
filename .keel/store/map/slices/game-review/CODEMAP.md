---
id: MAP-slices-game-review
scope: dir:slices/game-review
owner: agent
verified_at: 2026-08-26
generated: true
---

# CODEMAP — `slices/game-review`

<!-- generated by `keel map`. Do not edit: your changes will be overwritten. -->

7 files · 886 lines · 31 symbols

**`slices/game-review/features.js`** · 181 lines · imported by 3
- `function moverCp(cpWhite, turn)`  <sub>L19</sub>
- `function phaseOf(board, ply)` — opening plies = middlegame.  <sub>L27</sub>
- `function situationOf(evalBeforeCp, turn)`  <sub>L38</sub>
- `function moveFeatures(parentState, node)` — after it (carries from/to as 0–63 indices, captured, promotion, state).  <sub>L50</sub>
- `function isMateFor(score, turn)`  <sub>L62</sub>
- `function motifsFor(` — `toSquareName` is where the moved piece landed ('e4' style).  <sub>L70</sub>
- `function sideScore(result, side)` — the result is unknown.  <sub>L96</sub>
- `function gameOutcomeMotifs(moves, result, side)` — `moves` entries need { turn, situation }.  <sub>L105</sub>
- `function openingKey(sans, plies = 8)` — Grouping key for "which opening was this" — first N plies of mainline SAN.  <sub>L119</sub>
- `function tagMoves(mainline, results)` — Returns the review `moves` array stored on a library record.  <sub>L127</sub>

**`slices/game-review/classify.js`** · 139 lines · imported by 2
- `function evalToCp(evalObj)`  <sub>L20</sub>
- `function clampForMath(cp)`  <sub>L32</sub>
- `function computeLossCp(evalBeforeCp, evalAfterCp, turn)` — `turn` is the side that moved ('w' or 'b').  <sub>L41</sub>
- `function computeWinPctLoss(evalBeforeCp, evalAfterCp, turn)` — the engine) clamp to 0.  <sub>L53</sub>
- `function classifyByWinPctLoss(winPctLoss, opts =` — (0.10 / 0.20 / 0.30 of winning chance, scaled to a 0–100 percentage).  <sub>L64</sub>
- `function cpToWinPct(cp)` — Lichess win-percentage curve.  <sub>L76</sub>
- `function winPctLossToAccuracy(winPctLoss)` — Lichess per-move accuracy from win-percentage loss.  <sub>L82</sub>
- `function moveAccuracy(evalBeforeCp, evalAfterCp, turn)` — Per-move accuracy% given before/after eval cps and which side moved.  <sub>L90</sub>
- `function summarize(moves)` — `moves` is an array of { turn, lossCp, classification, accuracy }.  <sub>L98</sub>

**`slices/game-review/elo.js`** · 95 lines · imported by 3
- `function acplToRating(acpl)` — Monotone piecewise-linear interpolation over the table; clamped at ends.  <sub>L28</sub>
- `function gameAcpl(moves, side)` — mate-clamped by classify.js at review time.  <sub>L46</sub>
- `function estimateGame(moves, side)` — if the side has too few scored moves for even a rough read.  <sub>L54</sub>
- `function estimateAggregate(games)` — Returns { rating, band, games, acpl } or null if nothing qualifies.  <sub>L65</sub>
- `function formatEstimate(est)`  <sub>L79</sub>

**`slices/game-review/game-review.jsx`** · 191 lines · imported by 1
- `function fmtAccuracy(a)`  <sub>L15</sub>
- `function buildMovesFromTree(tree)`  <sub>L20</sub>
- `function GameReviewPanel(` — while either is analyzing.  <sub>L42</sub>

**`slices/game-review/analyzer.js`** · 167 lines · imported by 1
- `function parseInfoScore(line, fenTurn)`  <sub>L21</sub>
- `function useReviewWorker()`  <sub>L41</sub>

**`slices/game-review/batch.js`** · 82 lines · imported by 1
- `function reviewOneGame(record, reviewWorker, nodes)`  <sub>L17</sub>
- `function useBatchReview(reviewWorker, setLibrary)`  <sub>L43</sub>

**`slices/game-review/budgets.js`** · 31 lines · imported by 1

